server {
    listen 80;
    listen [::]:80;
    server_name govista.am www.govista.am;

    location /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
    }

    location / {
        return 200 "GoVista TLS bootstrap\n";
        add_header Content-Type text/plain;
    }
}
